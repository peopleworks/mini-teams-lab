<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>MiniTeams.Simulador</RootNamespace>
    <AssemblyName>Simulador</AssemblyName>
  </PropertyGroup>

  <!-- Desacoplado a propósito: NO referencia la app. Solo habla el contrato del hub por su nombre. -->
  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.SignalR.Client" Version="10.0.0" />
  </ItemGroup>

  <ItemGroup>
    <Content Include="guion.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
